output: build

meta:
  bolt:
    ensure_ascii: false

require:
  - bolt

data_pack:
  pack_format: 48
  min_format: 48
  max_format: 48
  description: §7The Stove SMP official RP Utils DP
  name: resource-utils
  zipped: false

resource_pack:
  pack_format: 64
  min_format: 15
  max_format: 64
  load:
    - src
  description: §7The Stove SMP official RP
  name: resources
  zipped: false

pipeline:
  - build
  - mecha